import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Essential Frailty Toolset | Cardiovascular Risk Calculator",
  description: "Medical calculator to evaluate 1-year mortality risk for elderly patients undergoing TAVR or SAVR procedures. Based on Harvard Medical School research.",
  keywords: ["frailty", "cardiovascular", "TAVR", "SAVR", "mortality", "risk calculator", "medical"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
